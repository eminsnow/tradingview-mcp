node_modules/
dist/
.env
.tv_session.json
*.js.map

# macOS metadata
.DS_Store

# Reverse-engineering probe output (per-session captures, can be large)
alert_capture.log

# Note: scripts/ is tracked (probe helpers live there).
